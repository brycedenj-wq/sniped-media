# OS WORLD-CLASS STACK , per deliverable
> minimum viable toolchain | world-class toolchain | what we have ACTIVE | what is missing | wire next

## 1. Premium still campaign
- MVP: Higgsfield (Nano Banana) + os_adobe_grade + os_postproduction_gate.
- World-class: + Adobe remove_bg/generative_expand for compositing + manual Photoshop for hero retouch.
- ACTIVE: all of MVP + Adobe crop. Missing: Adobe cutout/expand (AMBER), PS retouch (manual, later). Wire next: Sprint A.

## 2. Social campaign kit
- MVP: os_adobe_reframe + os_adobe_layout (carousel/thumbnail) + Adobe crop.
- World-class: + Adobe social-variations (multi-aspect subject-aware) + Airtable scheduling.
- ACTIVE: all of MVP. Missing: nothing critical. Wire next: nothing required.

## 3. Cinematic trailer
- MVP: Higgsfield Seedance (1 shot) + ffmpeg + still title cards. (= current internal teaser)
- World-class: 3-6 Seedance shots + After Effects-class motion graphics + sound design + grade-matched cards.
- ACTIVE: MVP. Missing: multi-shot workflow (Sprint B), motion-gfx + sound (LATER). Wire next: Sprint B for shots; AE-class is the real later gap.

## 4. Client pitch deck
- MVP: os_adobe_layout board/onesheet -> Pillow PDF. (current)
- World-class: Adobe document_render (InDesign) multi-page, typeset.
- ACTIVE: MVP PDF. Missing: InDesign render (AMBER). Wire next: Sprint A item 3.

## 5. Landing / proof loop
- MVP: os_adobe_layout landing PNG + parked form + os_form_score (local).
- World-class: real responsive HTML (/web-artifacts-builder) hosted on Netlify/Vercel + Airtable responses.
- ACTIVE: MVP (preview + scoring). Missing: real HTML + hosting (DEFERRED to public launch). Wire next: Sprint C (later).

## 6. Product / drop mockup
- MVP: os_adobe_layout + Pillow composite (current 2D card).
- World-class: Adobe remove_bg cutout for clean compositing + Blender for a true 3D wax-sealed certificate.
- ACTIVE: 2D MVP. Missing: cutout (Sprint A), 3D (Blender, LATER). Wire next: Sprint A cutout; Blender only for a real drop.

## 7. Motion / design system
- MVP: the locked grade + color-law + os_adobe_layout kit (a static system).
- World-class: After Effects-class animated brand system + Figma design tokens.
- ACTIVE: static MVP. Missing: animated system (AE, LATER), Figma (AMBER). Wire next: Figma test if a UI is needed; AE later.

## 8. Full world bible / IP system
- MVP: the world JSON + os_engine + docs (current).
- World-class: + Notion living bible + licensing one-sheets via document_render.
- ACTIVE: MVP. Missing: Notion (optional), InDesign render (Sprint A). Wire next: optional.

## 9. Private sales package
- MVP: os_engine kit + curated demo package (already built: OS_PRIVATE_DEMO_PACKAGE_001).
- World-class: + InDesign deck + a short real trailer.
- ACTIVE: MVP done. Missing: InDesign deck (Sprint A), trailer (Sprint B). Wire next: Sprint A.

## 10. Public launch (LATER)
- MVP: Netlify/Vercel + real HTML + domain + legal-final + payment.
- World-class: + analytics (Semrush) + email (Gmail/Twilio) + ads (Adspirer).
- ACTIVE: none of it (all HELD by policy). Wire next: nothing until a lane is proven and you say go.

## VERDICT
The world-class STILL/SOCIAL/DECK/PRINT-PROOF stack is mostly ACTIVE today. The cinematic and 3D stacks are the real "later" tiers. The cheapest, highest-leverage move is Sprint A (Adobe-MAX completion, 0 credits).
