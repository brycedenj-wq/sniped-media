# OS TOOL ACTIVATION PLAN , smallest sprint that closes the biggest ceiling gap
> Audit-derived. Nothing here is built yet. Ranked by ceiling-raise per unit of effort/risk.

## SPRINT A , "Adobe-MAX Completion" (RECOMMENDED FIRST. ~0 credits, low risk, high ceiling-raise)
Make the already-connected Adobe cloud layers ACTIVE with one proving artifact each (via the proven os_adobe_cloud upload handshake).
1. remove_background , cut out a DEED certificate / the wax seal -> a transparent PNG. Proves the cutout layer. (mockups, drop cards)
2. image_generative_expand , extend the DEED hero into a wide banner with masthead headroom -> a banner PNG. Proves generative canvas.
3. document_render_layout , build one minimal .idml and render a 2-page DEED deck -> an InDesign PDF. Proves world-class deck output. (needs a small template; the only real effort in this sprint)
4. document_merge , merge a 3-row CSV into a numbered-certificate template -> 3 numbered cert PNGs. Proves the edition-of-250 batch path.
Outcome: 4 Adobe layers AMBER -> ACTIVE, the Adobe claim becomes honest per-layer, and the DEED print-drop gets cutout + banner + numbered certs + a real deck. Cost: credits 0.

## SPRINT B , "Multi-Shot Motion Proof" (OPTIONAL, ~40-55 credits, within the 200 ceiling, 34 used)
Turn the internal teaser into a real trailer path.
1. Generate 2-3 more Higgsfield/Seedance shots (stamp-down, drawer-pull, certificate macro). (~18cr each)
2. Edit-pace them with ffmpeg/os_adobe_cut into a 15-20s cut. Proves the multi-shot trailer.
Gap that remains after B: motion graphics (animated titles) + sound. That needs an After Effects-class layer = LATER.

## SPRINT C , "Live Surface Readiness" (LATER, before any public launch, HELD)
Netlify/Vercel deploy test + /web-artifacts-builder real HTML landing + domain + legal finalize. Do NOT run until a lane is proven.

## DO-NOT-DO-NOW (distractions)
Blender, Unreal, Spline, Synthesia, Midjourney, Runway, CapCut. None closes a gap the current stack cannot cover for the product we sell.

## ORDER
A (now, free) -> decide lane -> B (if cinematic is needed for the pitch) -> C (only at public launch).
