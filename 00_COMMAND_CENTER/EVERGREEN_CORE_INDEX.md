# EVERGREEN_CORE_INDEX

**Date:** 2026-05-27
**Status:** The minimum readable core of the OS. Anchor-class: markdown-only, not chunked, not a chunk source, not in the master files. If the full corpus is unavailable or a future session starts cold, these docs alone restore the operating context. Under 1 MB total.

---

## 1. Read-first order
1. `CURRENT_STATE.md` :: where things stand, what is decided, the next actions.
2. `COMMAND_CENTER_RESPONSE_PROTOCOL.md` :: how to operate (route through the OS by job).
3. `TRUE_BILLION_DOLLAR_THESIS.md` :: what BASEPLATE is and the four layers.
4. `BASEPLATE_CANONICAL_STATEMENT.md` :: the governing public language.
5. `BASEPLATE_DOMAIN_AND_EMPIRE_ARCHITECTURE.md` :: name, domain, legal, deploy gate.
6. `INTERNAL_TIGHTNESS_BUILD_SEQUENCE.md` :: the build order to "internally tight."
7. `PROOF_LOOPS_30_60_90.md` :: the scoreboard.
8. `COMMAND_CENTER_SURVIVAL_AND_RECOVERY.md` :: backup and recovery.
Then the task-specific docs by job.

## 2. Minimal evergreen docs (the core set)
- **CURRENT_STATE** :: live operating state + next 7 actions + 90-day arc.
- **TRUE_BILLION_DOLLAR_THESIS** :: BASEPLATE = trusted operator layer of the physical-AI buildout. Wedge (Capability Dossier) to AI-Ops to named-author media to talent/capability network.
- **COMMAND_CENTER_RESPONSE_PROTOCOL** :: the OS is the default brain; classify level, route, receipt.
- **INTERNAL_TIGHTNESS_BUILD_SEQUENCE** :: the stranger-to-paid system and the build order.
- **PROOF_LOOPS_30_60_90** :: the proof scoreboard, T1/T2/T3.
- **BASEPLATE_CANONICAL_STATEMENT** :: governing language for every surface.
- **BASEPLATE_DOMAIN_AND_EMPIRE_ARCHITECTURE** :: KEEP BASEPLATE (wordmark held), flagship primary baseplateworks.com (canonical https://baseplateworks.com/; standard: .com first, no alt-TLD flagship; .studio/.systems redirect to it), employment gate resolved with guardrails.
- **COMMAND_CENTER_SURVIVAL_AND_RECOVERY** :: durability and restore.

## 3. Skills summary
- **/challenge** :: pressure-test a decision against the corpus, the locked doctrines, and the north star. Surfaces contradictions and drift. Read-only.
- **/save** :: persist an insight to the right home (memory / Command Center doc / log). Never chunks or mutates master files.
- **/boardroom** :: convene the advisor canon (Enns, Maister, Naval, Elberse, Thompson, Guidara, Sax, de Botton, etc.) to argue a decision from multiple lenses, then synthesize. Read-only.
- **/operator-review** :: apply the OS to an outside client/business/decision and return a diagnosis + proof-first plan, without exposing the private corpus. The first proprietary, client-facing skill.

## 4. Next-action map
- **Now (internal-first, BJ sets launch):** build the remaining internal-tightness artifacts in order (Discovery + Qualification next, then Proposal + Invoice, Sample Dossier, Proof Log SOP). Outbound playbook + admin SOP already exist.
- **Real-world steps owned by BJ:** register baseplateworks.com (the flagship), wire the site canonical + Calendly, build the sample dossier.
- **Then (on BJ's call, once internally tight):** run Phase 1 warm/manual outreach, log proof in PROOF_LOOPS.
- **Survival, today:** zip the ~10 MB precious layer to a private cloud + USB.

## 5. How a future AI session should recover context
1. Open this file and read the core set in order (section 1).
2. Read `CURRENT_STATE.md` for the live state and next actions.
3. Operate via `COMMAND_CENTER_RESPONSE_PROTOCOL.md`.
4. If memory is missing, restore it from the off-machine zip (see `COMMAND_CENTER_SURVIVAL_AND_RECOVERY.md` section 9 + `MEMORY_SNAPSHOT/`).
5. If working in another AI tool, paste `project_sniped_spine_portable.md` to restore the operating context.
6. Do not re-litigate the name, thesis, or domain unless the proof loop produces evidence that forces it.

## 6. What is parked (do not build now)
Platform / registry / verified-operator network (earned endgame). The app/product build. n8n / Twilio / RetellAI automation. Higgsfield/Kling content engine (concept-film is internal only). Clothing/status artifacts. Full ingestion of every book. Cold-email volume before warm proof.

## 7. Source of truth vs historical evidence
- **Source of truth (current):** the committed Command Center docs, led by `CURRENT_STATE.md`. When two docs disagree, the most recent committed one wins, and `CURRENT_STATE` arbitrates.
- **Historical evidence (not authority):** old SNIPED/BASEPLATE/brand-strategy docs, the April scratch docs, prior session saves. Useful context, never overriding the current committed docs.
- **Held:** the Bible (`SPIRITUAL_FOUNDATION`), until a deliberate decision.

## 8. Upgrade path
New resource: `raw/` intake to coverage audit to activation decision to skill/playbook update to commit to snapshot. New tool/model: point it at this index + `CURRENT_STATE`. Keep the evergreen core small; when a doc is superseded, update the pointer here so the read-first order stays current.

---

## Guardrails (unchanged)
Index/pointer doc only, no new strategy. No platform/registry/network public claim. No photography-only reduction. No automation before proof. Source of truth is the committed docs, not chat history. Anchor-class: not chunked, not in the master files, total_chunks unchanged at 1,837.
