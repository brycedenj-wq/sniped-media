# DEADPAN SUMMER · DRAFT v1 · FINAL VERIFY VERDICT (2026-06-13)
Harness: alma-final-verify (3 hostile judges whole-watched the 27-frame whole-film sheet + opus synthesis). Draft: 06_FULL_CUT/ALMA_DEADPAN_SUMMER_DRAFT_v1.mp4.

## VERDICT: NO-SEND. Score 4.5/10 (judges: vision 5.5, product 5.5, craft 4.5). FIXABLE, but needs targeted re-renders + a 4K sweep, NOT a grade/trim. All three judges = no-send, no dissent.

## WHAT IS SOLVED / STRONG (keep, do not re-render)
- SUIT held CORAL-ON-CREAM across all 27 frames, no mutation, cherry placed right. The hardest AI-film problem (product shapeshift) is SOLVED by the product-lock recipe.
- No readable text at contact-sheet scale (4K confirm still pending).
- Deadpan performance mode held; LUXURY/CHAOS architecture present.
- Strong beats to keep: rearview lipstick, palm-beauty (as image), walk-off back, trunk reveal, lens-wipe open, the END CARD (ships as-is).

## BLOCKERS (cannot send)
1. CAST IDENTITY FRAGMENTATION (catastrophic): the lead reads as 2-4 different women across the cut; recline-region + several INSERTS are off-model. ROOT CAUSE: the 8 first-pass inserts (b01,b02,b03,b04,b05,b08,b14,b16) were made BEFORE the clean-keystone/Element pipeline, so they do not match the re-done heroes. FIX: lock ONE identity with SOUL V2 (train on the clean keystone 2aef74e4) and re-render every off-model insert Soul-locked + Element-locked.
2. NECKLACE (non-spec SKU) on some front beats (carried from ref_front). Strip (gold hoops only).
3. FULL-RES (4K) TEXT/PLATE/SIGNAGE SWEEP mandatory before send (parked cars, rearview reflection).
4. WRIST BANDANA garment mutation on the hand-on-button insert (b04). Remove/re-render.

## MUST-FIX (below 9/10 floor)
5. Speaker-button insert (b04) rendered as a hand on a car hood, not the button. Re-render the correct macro.
6. Kick insert (b05) is a DESERT location, not the LA boulevard. Re-render in the established world.
7. TOWEL comedy peak (cut to stop-loss) is the THESIS of the piece per the judges, not optional. Restore it properly (plate-clean, necklace-free, concealment).
8. Leash POV (b08): fake circular "porthole" crop (AI tell) + red leash (spec = tan). Re-render flat wide POV.
9. Iced-drink prop on the poster beat (b18) competes with the suit. Remove.
10. SLIDESHOW MOTION: 60%+ of beats are near-static held pairs. Re-author start->end motion on the held pairs (this is the inserts again, and a couple heroes).
11. Palm-beauty over-held (trim), cigarette suit color slightly pink (grade pull ~5deg toward coral), speaker prop reads as luggage (re-stage).

## FIX PLAN (next block, full pipeline)
A. SOUL V2: train a Soul on the clean keystone (2aef74e4) for one locked lead. This is the identity lock not yet used; it ends cast fragmentation.
B. RE-RENDER the weak first-pass inserts (b01-b08,b14,b16) Soul + Element locked, correct beat/location/prop, authored micro-or-chaos motion per mode, necklace-free, no bandana.
C. RESTORE the towel beat (concealment, plate-clean) and the speaker-button macro.
D. Re-grade the few pink/over-held spots; remove iced-drink prop.
E. 4K export + full-res text/plate sweep.
F. RE-RUN alma-final-verify. Crown only on a clean >=9/10. send_no_send stays NO until then.

## STATUS: NO-SEND. send_no_send=no. The product-lock + heroes + end card are real; the inserts + missing towel + identity lock are the work. Draft v1 archived as a proof-of-pipeline, not a deliverable.
