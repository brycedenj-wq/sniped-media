# NEXT ACTIONS (your calls; nothing fires without them)
## Your manual steps to unlock more (no spend)
1. Select any frame in an open Figma /design/ file -> I run the Figma route live and classify it ACTIVE.
2. Give me the ElevenLabs key OR say "transcribe via Adobe credits" -> video-use transcription goes ACTIVE; the rest of the edit stack is already usable.
3. Decide Blender + AE MCP wiring: I prepared the gated steps (OS_BLENDER_SECURITY_GATE, OS_MCP_WIRING_PLAN). They need a Claude Code restart to load. Say go and I prep to the restart line.
4. Two safety closers (no spend): the Higgsfield $/credit rate + a private osbackup git remote URL.
## What I can do next autonomously (your word)
- Test Adobe generative-fill + document_render (InDesign deck) , Adobe credits.
- Build the proactive doctrine loop for visual/world (regenerate-until-pass).
- A multi-shot DEED trailer (HyperFrames titles + Seedance shots + video-use edit) , needs motion credits + transcription.
