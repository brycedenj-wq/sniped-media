# OS PRIME MOVER ARCHITECTURE , the OS as one organism (2026-06-05)
> The OS is not "Claude answering." Every output is the OS acting: input -> doctrine fusion -> toolchain router -> execution -> gates -> artifact -> logs -> dashboard -> learning loop. Everything enters; the input decides what wakes.

## THE SPINE (built + proven)
1. **Front door , os_prime_router.py** , maps any input to the MODULES that wake (18 organs). Tested: 7 inputs each woke the correct modules.
2. **Doctrine fusion , os_doctrine.py** , 9 domains, each fused to its certified books. `load` injects doctrine into creation (proactive); `check`/`fix`/`gate` verify+repair output (reactive+self-correcting). Wired into os_engine (clean copy at birth).
3. **Toolchain , os_tool_registry/router** , every tool classified (USE_NOW/TEST_NOW/HANDOFF/WIRE/INPUT_SPECIFIC/AVOID_IF_REPLACED), routed by intent. No access=capability.
4. **Execution , os_engine** , one intent -> full chain (grade/color-law/exports/layout/teaser/board/dashboard) + the module tools.
5. **Gates , os_postproduction_gate / os_motion_qa / os_privacy_gate / os_doctrine / os_blender_gate** , nothing ships ungated.
6. **Logs , EDIT_LOG / SPEND_LEDGER (two tanks) / gate logs** , every action + credit recorded.
7. **Dashboard , os_adobe_layout.dashboard / proof dashboards** , one glanceable state.
8. **Learning loop , failure ledger -> lessons -> next-time rules -> active standard** , every failure becomes a rule.

## HOW AN INPUT MOVES (worked example: "cut these clips into a trailer with sound")
- prime_router -> wakes [editing, video_motion, sound].
- editing loads motion doctrine, routes video-use + ffmpeg, gates os_motion_qa.
- video_motion loads motion doctrine, routes Higgsfield/HyperFrames.
- sound loads motion doctrine, routes Firefly-sound (handoff).
- execute -> gate (os_motion_qa + os_doctrine:motion) -> artifact -> log -> dashboard -> learning.

## THE LAW
The OS may never say "we do not need this." It says: USE NOW / TEST NOW / HANDOFF / WIRE / INPUT-SPECIFIC / AVOID-ONLY-IF-REPLACED. Every capability proves itself with an artifact. The input chooses the stack. The OS moves as one thing.
