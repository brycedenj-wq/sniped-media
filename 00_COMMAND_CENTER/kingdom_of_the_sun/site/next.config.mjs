/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // Local images live in /public and are served by Next's image optimizer on Vercel.
};

export default nextConfig;
