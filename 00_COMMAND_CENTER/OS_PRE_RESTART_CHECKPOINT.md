# OS PRE-RESTART CHECKPOINT , tool-environment refresh (2026-06-05)
> Read FIRST after restart. State is saved, tree clean. No background jobs running. Safe to restart Claude Code / Claude Desktop / Blender / Terminal. First task post-restart = TOOL VERIFICATION PASS (no creative work until verified).

## PROVEN THIS SESSION (artifact + commit)
| capability | proof artifact | commit |
|---|---|---|
| OS Prime Mover (os_prime_router, 18 modules) | TEST_RESULTS (7 inputs -> right modules) | 923d376 |
| Doctrine fusion (os_doctrine, 9 domains, clean-at-birth) | DEED doctrine_copy PASS | 8fd2b94 |
| HyperFrames (motion-graphics) | deed_animated_title.mp4 | 923d376 |
| Blender gated socket route | blender_proof.png (cube render) | 22cd386 |
| Adobe-MAX (crop/cutout/Firefly expand) | DEED 1:1, cutout, banner | (everything-stack) |
| os_engine (one-intent chain) | DEED/REMAINS kits, gate SHIP | 8fd2b94 |
| os_campaign (one-command kit) | LOT00/DEED kits, 22/22 tests | (prior) |
| privacy gate | audit SHIP 0 leaks | (prior) |
| tool router (8 routes) | 8/8 route tests w/ artifacts | (prior) |

## STATUS
- ACTIVE (callable here, proven): Higgsfield (stills+video), Adobe for Creativity (crop/cutout/expand, +credits), HTML+Chrome render, HyperFrames, ffmpeg/Pillow/exiftool/git, os_* suite (engine/campaign/grade/composite/reframe/layout/teaser/cut/gate/doctrine/prime_router/tool_*/privacy/money/launch/blender_socket+gate), Airtable (read).
- AMBER (connected/staged, one step short): video-use (transcription key), Figma (needs a frame selected), Notion/GDrive/Gmail/Semrush (connected, unproven route), Adobe generative-fill + InDesign render (test pending), multi-shot trailer.
- RED / not callable here: Blender NATIVE MCP server (registered in Claude DESKTOP, not Claude Code), After Effects MCP (not wired), Midjourney/CapCut/Synthesia/Unreal/Spline (not installed). Netlify needs re-auth.

## EXACT COMMAND/CONFIG TO ADD BEFORE RESTART (optional, your call)
To make the Blender NATIVE MCP server visible in Claude Code after restart:
  claude mcp add blender --scope user -- /Users/sniper/.local/bin/uv --directory ~/blender_mcp/mcp run blender-mcp
SECURITY NOTE: the NATIVE MCP tools would bypass os_blender_gate (the gate only wraps os_blender_socket.py). The gated socket is the SAFER route and already works. Register the native MCP only if you want it, and rely on Claude Code's own per-tool permission prompts.
(After Effects MCP, if wanted: clone Dakkshin/after-effects-mcp -> npm install/build/install-bridge -> claude mcp add after-effects -- node <path>/build/index.js -> open AE panel. Not prepared yet; say the word.)

## POST-RESTART TOOL VERIFICATION PASS (run these, confirm, no creative work)
1. `claude mcp list` , confirm reconnect: Higgsfield, Adobe, Airtable, Google x3, Notion, Semrush, Figma, Vercel. Re-auth Netlify if needed. Confirm whether `blender` now appears (if you added it).
2. Adobe: one safe op (e.g. crop) returns an artifact.
3. Higgsfield: `balance` returns (Higgsfield tank) ; Adobe credits (3990) noted separately.
4. Blender: `python3 00_COMMAND_CENTER/scripts/os_blender_socket.py proof` re-renders (socket route), OR test the native MCP if registered.
5. Figma: select a frame, run get_design_context.
6. Local: `python3 00_COMMAND_CENTER/scripts/test_postproduction.py` (expect 22/22) + `os_prime_router.py route "..."`.
7. Update CAPABILITY_STATUS.csv with the verification results.

## SAFE TO RESTART
- Claude Code: YES (work committed, no bg jobs). New session reads OS_SESSION_CONTINUITY.md + this file.
- Claude Desktop: YES (separate app; if you registered Blender there, it stays).
- Blender: YES to keep open (the proof scene is a throwaway in-memory scene, not saved). Re-open the add-on panel + start the server (9876) after any restart.
- Terminal: YES.
