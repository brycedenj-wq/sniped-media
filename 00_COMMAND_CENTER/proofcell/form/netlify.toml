# Netlify drop config. To deploy: drag the `site/` folder onto app.netlify.com/drop (your account).
# Do NOT connect a custom domain. Do NOT use a real-name team. Private preview only.
[build]
  publish = "site"
