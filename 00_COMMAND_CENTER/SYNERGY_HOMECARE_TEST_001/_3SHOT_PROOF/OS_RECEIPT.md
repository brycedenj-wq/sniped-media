# OS_RECEIPT - Synergy 3-shot competence proof

## Layer 1 - scan
- domain film | serious=True | mode=MAX. Harness-mandatory: generation on main thread (Higgsfield connector), judging routed to a fresh-context harness (no self-crown).
- Activated: REAL_FILM_PRODUCTION_OS (Push-In Law, verb-first, tool-per-shot), os-face-lock (locked stills as i2v keyframes), os-world-bible (one home, one grade), os-vision-reject-gate (slop hunt), watch (whole-watch), Higgsfield MCP (Kling 3.0 i2v pro + nano_banana_pro), COMMERCIAL_CRAFT_BENCHMARK_V2 (scoring), MASTER_OS_CONDUCTOR + MAX mode.
- Asleep (by design): Premiere/AE/Blender (no edit/composite needed for a motion proof), Seedance (face-filter + NSFW-false-positive risk on tender beats -> Kling chosen), ElevenLabs/Suno (sound is a finishing pass; proof is silent on purpose).

## What CHANGED because the OS activated
- The proof was aimed by the prior THE_DOOR_FILM_AUDIT at the EXACT three beats the film failed (performance as frozen stills), instead of generating random pretty shots. The no-sampling whole-watch + a 16-agent fresh-context adversarial harness then judged it, and CAUGHT the hero shot falling back into a push-in (fr_001 == fr_028) that a self-review would likely have crowned. The OS refused to call a frozen-subject push a film shot.
- It also surfaced the real, reusable finding: identity-lock and anti-slop are SOLVED with Kling 3.0 i2v from a locked still; the only failure mode left is performance-absence, fixable by END keyframes + performance-verb prompting.

## Gates passed / failed (v1)
- PASS: os-face-lock (identity stable all 3), os-vision-reject-gate (no slop all 3), client-safe tone (anti-gloss documentary all 3), Push-In Law on shots 1 + 3 (real subject change).
- FAIL: Push-In Law on shot 2 (animatic-fail, frozen subject under a push). 9/10 floor not met (avg 6.17). Owned music absent (finishing gap).

## Rating + why
- 8.0 / 3.0 / 7.5 (avg 6.17), adversarial. Two real cinema beats, one animatic-fail hero. Not a slop problem; a performance-direction problem.

## What blocks 10/10
- Shot 2 must perform a real recognition arc (END keyframe + performance verbs); shot 3 should stage the literal turn; shot 1 needs a legible decision micro-beat. Plus owned music (Suno) + diegetic sound design in finishing.

## VERDICT (updated again after the EXCELLENCE pass) - BLOCKED
**Excellence pass (Option 2) did NOT pass. Do not scale.** Target was all 3 >=8.5, hero >=9. Result: best-of ~7.67 (S1 v1 8.0, S2 v2 7.5, S3 v1 7.5); hero ceilinged at ~7.5 across four attempts (v1 3.0, v2 7.5, v3 7.3, v4 6.5). The v3 polish REGRESSED the set (S1 eyes-closed end keyframe read as sleep + phone morph -> 6.8; S3 start frame never showed the window so the turn was impossible + push-in -> 3.5). The hero's binding gaps: text-keyframe welling/tears do not render convincingly and Kling defaults to a small softening + push rather than a big felt delta. Identity-lock + anti-slop stayed clean throughout. Next is an OPERATOR DECISION (hand-edited tear end-frame vs hero-beat pivot vs accept the restrained read which scored 8.7 client-safe), not another blind reroll. Credits protected at ~65. send_no_send=no. Full detail + method card: 01_PROOF_PACKET.md.

## (superseded) VERDICT after the first method fix
**COMPETENT = YES / proceed-to-full-film (with conditions).** v1 caught the hero animatic-fail; the END-keyframe + performance-verb method fix was applied to Shot 2 and RE-VERIFIED by a fresh-context harness (run wf_8d43b463-013): the hero jumped from 3.0 animatic-fail to 7.5 cinema-pass, endpoint-diff passes, method_fix_works=TRUE. Proof now passes at the cinema bar 3/3 (8.0 / 7.5 / 7.5, avg ~7.7), all identity-locked + slop-free + client-safe.

The competence question (real cinema vs stills-panning) is PASSED. The reusable method is proven: start-still face-lock -> authored END keyframe -> Kling 3.0 i2v start->end with performance verbs + camera held -> adversarial endpoint-diff gate -> 2-3 takes select.

Conditions before any CLIENT send (not competence blockers): per-beat lift to the 9 floor (peak-hold + ocular bloom on shot 2; stage the literal turn on shot 3; decision micro-beat on shot 1), owned music (Suno) connected, diegetic sound design. This proof is an INTERNAL competence proof, send_no_send=no. Full-film build is a new authorized-spend phase awaiting operator greenlight.

---
## VERDICT (best-of brute-force pass, 2026-06-08 night) 
VERDICT: blocked

Best-of brute-force pass (unlimited-image keyframes + 5-role selection harness + best-of verify, runs wf_07439aad-933 / wf_4d256ae0-f43). NOT deliverable. Overall 6.2, story does not read across the 3 beats.
- SHOT1 daughter v1: 6.5 (was 8.0 on a looser panel). Fresh strict panel caught a CASTING INVERSION: the "daughter" reads as a gaunt late-50s/60s care-RECIPIENT, breaking the daughter-worry beat + an unwell waxy sheen. Fix: re-cast/re-prompt a 30s-40s daughter, warm the phone glow, add a decision micro-tell.
- SHOT2 Eleanor recognition v5: 7.5. The blank-profile -> turn -> warm almost-smile ARC now works (is_cinema true; brute-force keyframe ELE_smile_1 fixed the too-internal blocker). Capped by a mid-turn mouth-gape (fr_010-013 slack jaw reads as speech). Fix: one targeted reroll, eyes-first + mouth CLOSED (MID keyframe lips-closed, negative-prompt open-mouth). One reroll from 8+.
- SHOT3 together v2: 4.5, not cinema. Continuity-lock + closer framing worked, but the lead never turns (END keyframe kept her heads-down) so the connection beat never stages + hand-slop blob. Fix: END keyframe with a REAL head-turn + eye contact, hand-region cleanup.
- Music: all 3 silent; Suno not connected; scratch is not final. Music handoff required.

What CHANGED because the OS activated: the unlimited-image brute-force (24 candidates, scored by 6 fresh agents) + the derive-END-from-START continuity fix + the strict best-of panel caught a casting inversion and a connection-beat-absence that looser passes missed, before a partner ever saw it. The hero arc materially improved (v2/v4 -> v5 real turn). No shot was self-crowned.

Path to deliverable (next pass, all keyframe-first then 1 video reroll each): (1) re-cast daughter younger + micro-tell, (2) Eleanor eyes-first mouth-closed reroll, (3) together real-turn END + hand cleanup, (4) re-verify, (5) connect Suno + score. send_no_send=no.
