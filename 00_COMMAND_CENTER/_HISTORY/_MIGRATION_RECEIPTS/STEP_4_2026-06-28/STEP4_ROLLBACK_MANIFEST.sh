#!/usr/bin/env bash
# Reverse Step 4 (run from repo root). OS_EVERYTHING_STACK was tracked -> git mv back.
set -e
git mv 00_COMMAND_CENTER/_HISTORY/OS_EVERYTHING_STACK 00_COMMAND_CENTER/OS_EVERYTHING_STACK
