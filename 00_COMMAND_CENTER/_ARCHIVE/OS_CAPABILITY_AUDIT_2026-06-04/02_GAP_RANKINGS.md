# 02 GAP RANKINGS

## Top missing by DANGER (what hurts most if left broken)
1. **backups (offsite remote)** , Verified NONE: no git remote 'osbackup', no /tmp/os_backup.log. Backups are local-commit only. A laptop loss or repo corruption wipes the entire brain (1260 sources, all skills, all ledgers) with zero recovery. Highest-consequence, fully irreversible, and closeable in two commands.
2. **privacy/identity/employer-risk gate (enforced)** , os_privacy_gate.py does NOT exist (verified); only a manual PRIVACY_CHECKLIST.md. For a field-engineer-with-employer, a real-name/employer leak via form-owner field, WHOIS, analytics pixel, or asset EXIF is hard to reverse. The one risk the operator explicitly guards has no enforced gate before SHARE.
3. **monetization/payment** , Verified NONE: no payments/ folder, no link, SCORE.md never generated. The machine can manufacture demand but has zero path to capture money, so a hot lead arrives and dies uninvoiced. Danger here is opportunity-loss plus the temptation to improvise an unsafe real-name rail under pressure.
4. **cost: USD rate on the ledger** , .prod_cost_rate absent (verified): every USD reads UNKNOWN. The cost guard blocks runaways in credits, but spend is invisible in dollars, so a proof loop firing real generation can drift past a budget no one can see in money terms.
5. **command router (untested classification)** , AMBER, built_untested. No fixture set proves the router puts a request in the right mode. A misroute means strategy crowns a lane, execution invents strategy, or research ships uncited, with no routing receipt to audit why. Dangerous because it silently corrupts the mode of every downstream answer.
6. **dashboard/control-room (populated)** , AMBER, untested: campaign_house PROOF_LOOP_DASHBOARD.md tables are header-only. When the loop fires, keep/kill/scale signal lives only inside SCORE.md with no tracked control surface, so the operator re-derives state each session and a kill signal can be missed.

## Top missing by LEVERAGE (most output/quality/money per unit of build)
1. **os_adobe_asset.py (shared upload/search/pull I/O)** , Foundational plumbing every other Adobe wrapper depends on (upload local TIFF, get asset id, run edits, pull result). One small build unlocks grade, cutout, reframe, and composite lanes at once. Highest leverage-per-line because it is the single dependency under five high-value wrappers.
2. **os_adobe_reframe.py + SNIPED_EXPORT_SPECS.json** , The 9 locked export presets are already fully specced on disk (preset_library.md s4, lightroom_operating_system.md s8), zero new decisions. One hero in, the full IG/LinkedIn/print master set out. Cleanest first Adobe dry-run, directly multiplies output per shoot.
3. **os_adobe_grade.py + SNIPED_LUXURY_GRADE.json** , The v3 LUXURY slider math already exists in SNIPED_LOCKED_LOOK_v3_LUXURY.xmp; encoding it into a 10-step image_adjust_* chain converts the manual grade into a repeatable batch pass. Makes every generated/shot frame recognizably SNIPED without manual Lightroom, the core authorship multiplier.
4. **landing/form deploy (one endpoint paste)** , The HTML + 1.27MB hero + 1.95MB loop are done; only action= endpoint and host remain. A 2-minute paste starts the proof clock and gives the GREEN ingestion/scoring engine its first real input. Tiny build, unlocks the entire Rails A/C demand side.
5. **os_proof_dashboard.py (~30 lines)** , Reads RESPONSES.csv + SCORE.md and writes the populated dashboard row. Closes the gap between the GREEN scoring layer and the empty static tables, turning scattered signal into one glanceable verdict surface for near-zero build.
6. **os_edit.py (Adobe video resize/cut wrapper)** , Wraps video_create_quick_cut + video_resize into one logged, gated workflow writing EDIT_LOG rows. Takes the dead EDIT stage live, makes every motion clip platform-ready, and one logged dry-run flips the whole Adobe-edit umbrella from AMBER toward GREEN.

## MUST build BEFORE any public proof loop
- Backups offsite: set git remote osbackup + one real push so the brain has a copy that survives laptop loss. Verified NONE today (no osbackup remote, no /tmp/os_backup.log). A public proof loop that draws real people while the only copy of the OS is local is the single most irreversible exposure.
- Privacy/identity/employer gate enforced (os_privacy_gate.py): exiftool strip on all share assets + a grep refuse on banned tokens (real name, 'sniped', employer, personal handles) in any deployed HTML/form. Verified NONE (script does not exist; only PRIVACY_CHECKLIST.md prose). For a field-engineer-with-employer this leak is hard to reverse.
- Form endpoint scrubbed for owner-field / real-name leak BEFORE deploy: index.html still has action=REPLACE_WITH_YOUR_FORM_ENDPOINT, and the form-owner field, WHOIS, and analytics pixel are exactly the leak surfaces. The privacy gate (item 2) must run against the actual deployed endpoint, not the stub.
- Legal NOW-stubs (ToS, Privacy, NDA, IP-assignment) moved out of the OS_MAX_DEMO_001 sandbox into a live 00_COMMAND_CENTER/legal/ folder, with the two missing files (_stub_nda.md, _stub_ip_assignment.md) actually written. Verified NO live legal/ folder. Going public named + sharing AXIS/MERIDIAN IP without these risks losing the core owned asset.
- Cost guard + cost ledger confirmed live before any spend the loop triggers: os_cost_guard.py is GREEN/tested and the cost ledger exists, but .prod_cost_rate is absent so every USD reads UNKNOWN. Set the rate so a runaway is caught in dollars, not just credits.
- Completion/stop gates confirmed firing (they are GREEN) so the loop cannot claim 'done' on an unread scope or crown a lane on the first 3 responses.

## Can WAIT (safe to defer)
- Premiere/After Effects multi-clip edit layer (RED): no second QA-passed clip exists to cut yet. Single-clip motion already ships QA'd. Defer until there are 2+ approved clips to assemble.
- Illustrator/InDesign layout layer (RED, greenfield): Op Kit one-pager and drop cards are manual-GUI today and that is survivable through first proof. Layout is a deliverable-polish lane, not a proof-loop prerequisite.
- Speech/audio cleanup (media_enhance_speech): no current workflow demands it; stills are the active revenue surface. One-line routing note is enough.
- Higgsfield video credit-rate confirmation (ASSUMED_VIDEO_RATE_CR_PER_SEC=None): motion is the secondary lane; prep-video refusing to estimate is a safe failure, not a dangerous one.
- Face-embedding ONNX model swap for os_facematch auto-PASS: today every match escalates to human vision read, which is the SAFE direction (it can FAIL/QUARANTINE automatically, only PASS needs a human). Conservative, can wait.
- 68 INSTALLED_INCOMPLETE skills reaching ACTIVE: per-skill content work, not infra. The 5 ACTIVE skills cover the live pipeline. Backlog, not blocker.
- Per-file segment ledger to move 82% provisional word-volume to certified: important for truth-layer honesty but the corpus is already 72.2% read_verified and the certify script is GREEN. Hardening, not a proof-loop gate.
- Notion/Airtable remote mirror of the dashboard and calendar: local CSV + markdown is enough to start. Remote control room is convenience.

## NEVER automate (taste or risk demands a human)
- **Identity layer of any composite or grade (face/body/skin texture)** , Locked doctrine: identity-AI is a hard refusal on client deliverables. The CRS hard-invariants and the COMPOSITE_MASTER_QA G1 biological-reality gate exist precisely because an automated identity edit is both the IP/legal risk (accidental celebrity-likeness leakage) and the brand-integrity risk. The machine may grade, cutout, reframe, and gen-fill the WORLD, never the person.
- **Lane-crowning / final strategic direction** , Doctrine: proof decides authority, no lane crowned from a confident doc or partial engagement. The OS possibility-engine rule forbids collapsing Bryce into one identity. A model auto-promoting a direction off thin signal is the exact failure the proof-before-crowning gate guards. Keep the keep/kill/scale READOUT automated, the CROWN human.
- **Going-public trigger (form deploy, named brand, IP share)** , Employer/identity overlap is the highest-consequence, least-reversible risk for a field-engineer-with-employer. The privacy and legal gates can flag, but the decision to expose a real surface must stay a deliberate human act, never a script side-effect.
- **Payment link creation and real-name/entity exposure on it** , Payment-follows-proof doctrine plus the EIN/entity staging means the moment money and a legal name attach to a public surface is a one-way door. A human sets up the legitimate link in their own account; the OS tracks revenue-in after, it does not provision the rail.
- **Final human-taste sign-off on any client-ready composite or graded hero** , Eleven layers carry needs_human_taste. The strongest-photograph reject gate can require output beat the source, but believable/client-ready is a taste verdict (floating shadows, cutout tells, sensor mismatch). The scorecard informs; a human signs client-ready.