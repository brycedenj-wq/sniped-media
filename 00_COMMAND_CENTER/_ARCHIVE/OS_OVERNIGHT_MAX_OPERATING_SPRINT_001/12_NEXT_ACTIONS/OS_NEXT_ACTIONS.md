# 12 NEXT ACTIONS , queued, ranked (nothing here performed without your go)
## Cheap safety closers (no spend, flips launch-check FAILs)
1. Set USD cost-rate: `python3 scripts/os_cost.py rate set --usd-per-credit <X>` , give me the Higgsfield $/credit. (cost_rate FAIL -> PASS)
2. Offsite backup: add a private `osbackup` git remote URL (you create the repo; I cannot make accounts) then I push. (offsite_backup FAIL -> PASS)
3. Legal: take the DRAFT stubs in `00_COMMAND_CENTER/legal/` to a real lawyer to finalize. (stays legal-review-needed)
## Creative pushes (some spend, your pick)
4. Adobe-MAX: upload the DEED 4K hero into Adobe storage (asset handshake), then run image_crop_and_resize (subject-aware social set), remove_background (drop mockup), generative_expand (banner). Promotes Adobe-MAX AMBER -> ACTIVE.
5. Multi-shot DEED trailer: 2-3 seedance shots (stamp down / drawer / vault) cut with os_adobe_cut into a real trailer (~40-55cr).
6. Build the private DEED print-drop proof-loop offer (internal, no launch): edition-of-25 page + the parked form wired behind Privacy+ToS.
## Tool bridges (optional)
7. Bridge one claude.ai-app plugin into the CLI via /mcp-builder only if a route needs it (most are HELD anyway).
8. Route-test Airtable or Notion once to promote it AMBER -> ACTIVE (lead tracking).
