# THREE FACTS BEFORE ACTION

The campaign is fully built. Three facts are still needed before any action. The moment the operator drops these three values, the propagation table in `V2_BUILD/TRIP_CAMPAIGN_V2.md` §A.2 fills every slot in one pass, no rebuild.

The job stays the same: open ONE real room and prove the 10k Campaign Universe Buildout offer. These three facts do not change the job; they make the built machine concrete (`OFFER_PROTOTYPE.md` proof section).

Constraints: zero spend, zero generation, zero posting, no naming, no crown, no em-dashes, no hype.

---

## THE THREE FACTS

1. `{DEST}` (destination, the city or region the trip lands in)
   Sets: the geography and scenes, the candidate-sourcing channels, the screened target roster, the `{THRESHOLD}` candidate set, the location rig and wardrobe lane (`V2_BUILD/TARGET_DISCOVERY_METHOD.md`).

2. `{DATES}` (start and end, as real calendar dates)
   Sets: every `{DAY_N}` in the runbook, the pre-warm window, the on-ground loop count, the trip deadline, the mid-trip ladder-trigger date, and the event-calendar window for sourcing (`V2_BUILD/DAY_BY_DAY_RUNBOOK.md`).

3. `{AUDIENCE}` (platforms, rough size, how warm)
   Sets: the post routing and pillar ratio, the already-warm-in-location bucket, and every numeric threshold and kill number in the proof dashboard tiers 2, 3, 4 (`V2_BUILD/PROOF_DASHBOARD.md`).

None of the three depends on the other two. Drop them in any order; the derived set (`{TARGET}`, `{THEIR_THING}`, `{THEIR_MOMENT}`, `{DAY_N}`) resolves in dependency order after.

---

## ALREADY RESOLVED (do not re-ask)

- OCCASION: resolved. Operator-chosen proof trip, no fixed event unless the operator names one (`V1_BUILD/INTAKE_FILLED_AND_LEDGER.md` field 3).
- OFFER FORK: resolved. Campaign Universe Buildout is the active offer, 10k floor / 25k / 50k+. BASEPLATE is parked as a one-line redirect, not active (`V1_BUILD/INTAKE_FILLED_AND_LEDGER.md` ledger A1; `V2_BUILD/TRIP_CAMPAIGN_V2.md` §B.6).

---

## THE BOUNDARY

Condition-met is eligible, not authorized. After the three facts drop and the campaign resolves to concrete, every send, post, generation, and naming still runs only on the operator's explicit go (`V1_BUILD/TRIP_CAMPAIGN_V1.md` status). Drop the three values to make it concrete; that is not the same as turning it on.

---

## SOURCES CITED
- `00_COMMAND_CENTER/TRIP_CAMPAIGN_OS_001/V1_BUILD/INTAKE_FILLED_AND_LEDGER.md` fields 1, 2, 3, 6, ledger A1
- `00_COMMAND_CENTER/TRIP_CAMPAIGN_OS_001/V1_BUILD/TRIP_CAMPAIGN_V1.md` status
- `00_COMMAND_CENTER/TRIP_CAMPAIGN_OS_001/OFFER_PROTOTYPE.md` proof section
- `00_COMMAND_CENTER/TRIP_CAMPAIGN_OS_001/V2_BUILD/TRIP_CAMPAIGN_V2.md` §A.2, §B.6, §C
- `00_COMMAND_CENTER/TRIP_CAMPAIGN_OS_001/V2_BUILD/TARGET_DISCOVERY_METHOD.md`
- `00_COMMAND_CENTER/TRIP_CAMPAIGN_OS_001/V2_BUILD/DAY_BY_DAY_RUNBOOK.md`
- `00_COMMAND_CENTER/TRIP_CAMPAIGN_OS_001/V2_BUILD/PROOF_DASHBOARD.md`
