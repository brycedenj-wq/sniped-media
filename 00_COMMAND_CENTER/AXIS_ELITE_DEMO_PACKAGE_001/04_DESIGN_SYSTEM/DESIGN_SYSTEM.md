# DESIGN SYSTEM (artifact: design_system_board.png)
- Type: display serif (Didot/Bodoni lane) for masthead (single word) + grotesk (Helvetica/Neue Haas) for kickers/captions/UI. Type scale on board.
- Grid: 12-col, 60px margin, 8pt baseline.
- Color tokens: ink #141315, bone #E8E4DC, graphite #4F5258, oxblood #5A1A1A (ONE-accent law).
- Mark usage: blind deboss; oxblood only on wax.
- Layout rules: poster (single-word masthead, full-bleed image, one line + mark), one-sheet (wide hero + title + logline + details + mark footer), board (6-frame range, never the same shot twice).
- FIGMA STATUS: system specified + rendered as a board artifact here. Live Figma file build (use_figma) requires the figma-use skill + an open Figma file handshake , QUEUED as a next step; the spec above is build-ready for Figma. Logged honestly, not faked.
