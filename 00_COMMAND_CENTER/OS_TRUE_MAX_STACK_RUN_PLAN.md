# OS TRUE MAX STACK RUN PLAN
### The next elite run: AXIS rebuilt on the full premium stack. Spend-on, gate-enforced.
Prepared 2026-06-05. Target: move AXIS from CLEAN BUT GENERIC (17/36) to ELITE (>=30, every dim >=2, no leak).

## Pre-flight (this run is authorized as a real MAX run)
- `os_premium_stack_gate.py plan film` = FULL PREMIUM STACK REQUIRED (all tools ACTIVE).
- One manual setup may block Figma/Blender write paths (see "setup blockers" below). If so: HOLD that stage, justify, continue.

## The 15-stage plan with tool, action, and credit posture

1. **Concept/world** , deepen MERIDIAN: add tension + a symbol system (a mark + one oxblood object = "what the house keeps") + lineage specificity. Doc only. 0 cr.
2. **Hero generation** , Higgsfield nano_banana_pro 4K, ref=locked hero. Keep cand_02 + 1 new beauty/face frame. ~12-20 cr.
3. **Still range (shot taxonomy)** , generate the 6 types NOT yet shot: beauty/face, still-life of the symbol object, detail/macro (texture + oxblood), true wide, gesture/hand, second-motif. ~40-64 cr.
4. **Spatial continuity** , Blender: build ONE brutalist set, light once, render wide/detail/over-shoulder plates with true continuity (kills the AI-architecture tell). Gate via os_blender_gate, sandbox. ~0 cr (compute), setup time.
5. **Adobe cloud post** , remove_bg the symbol object to composite; generative_expand a cinematic wide; Lightroom-grade for a single true accent. Adobe credits.
6. **Figma design system** , display+grotesk pairing, grid, the AXIS mark, components. Replace Pillow template. Fixes the LOT00 leak at the source.
7. **Layout/spread** , Adobe InDesign render (or Figma) for a real 3-spread editorial, not a card.
8. **Titles/motion graphics** , After Effects/HyperFrames kinetic type for open + symbol reveal + end.
9. **Edit/pacing** , assemble range + motion beats + symbol-reveal hook; real pacing, not concat.
10. **Sound/silence** , decision logged. Default: designed silence + one deliberate hit (sound gen is HANDOFF).
11. **Proof dashboard** , Figma/layout dashboard of the run.
12. **Operating layer** , Notion project room (AXIS world wiki) + Airtable asset/proof registry.
13. **Delivery** , Drive archive + sellable PDF/Figma. No public host.
14. **Gates** , premium-stack audit + elite gate (target ELITE) + post-prod + privacy + copy.
15. **Failure ledger** , log any skip/underuse.

## Setup blockers to confirm before the run (tell me which to clear)
- Figma write (use_figma) needs a live file + the figma-use skill , confirm Figma desktop/file ready.
- Blender native render writes to MCP temp -> OS copies to sandbox (known, handled).
- Adobe cloud asset ops need the os_adobe_cloud upload handshake per asset (proven).

## Budget posture
Estimated generation 50-90 credits + Adobe monthly credits + Blender/Figma compute. Within authorized run budget; refill if the work justifies. Stop a direction if it scores weak; push a direction if it scores strong.

## Definition of done
Elite gate STRONG minimum (ELITE target), premium-stack audit = FULL PREMIUM STACK USED or PARTIAL JUSTIFIED, post-prod SHIP, privacy SHIP, 0 template leaks. Only then is it a true MAX run.
