# 11_MEMORY_AND_HANDOFFS · the memory map and handoff patterns

A pointer system, not a memory dump. There is a REAL dual-root memory split on disk (verified 2026-06-14). Both stores are populated with different content. Reconciliation is an open operator call.

## Memory stores (TWO, both populated, REAL split)
- `/Users/sniper/.claude/projects/-Users-sniper/memory` (about 83 items): feedback rules, intel frames, capability-growth notes.
- `/Users/sniper/.claude/projects/-Users-sniper-AI-Brain-Refinery/memory` (about 39 items): AI cinema production, Alma campaign, Higgsfield doctrine, project notes.
- Each store has a MEMORY.md index loaded at session start.
- DECISION GATE (open call, operator only): merge into one, keep both intentionally, or deprecate one. Do not merge before reading both in full or feedback rules can be lost. The counts above are approximate; confirm exact counts when reconciling.

## Handoff patterns
- Per-project handoff: `HANDOFF_YYYY-MM-DD.md` inside the project folder (example: `00_COMMAND_CENTER/kingdom_of_the_sun/HANDOFF_2026-06-01.md`).
- Session snapshots: auto-generated by the session-save skill; index at `00_COMMAND_CENTER/MEMORY_SNAPSHOT/README_MANIFEST.md` and `00_COMMAND_CENTER/session_saves/`.
- Cross-project frames: the MEMORY_SNAPSHOT manifest.

## Reading order for resuming context
HANDOFF for the project, then that project's memory, then OS_ACTIVATION_INDEX.json to route the new task.

## Known gaps
Projects without a HANDOFF doc (Alma, Prime Mover, the Gemini review lane). Missing memory frames for some archived proofs.

Updated by: manual on project start (create the HANDOFF) and on a major context shift. Session-save outputs are auto-generated. The memory reconciliation is operator-gated (read both stores first).
